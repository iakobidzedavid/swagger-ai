# Token Conformance Audit

**Generated by:** `scripts/audit-tokens.mjs`
**Scope:** all `.tsx`/`.ts` files under `src/app` + `src/components` (excluding `src/app/**/api/*` route handlers, which have no UI colors)

## Methodology

1. Parse every `--token: #hex;` declaration out of `src/app/globals.css`'s `:root` block.
2. Walk every page/component file and regex-match raw hex color literals (`#fff`, `#0d1f33`, etc.).
3. Classify each literal:
   - **Token match** — the hex value is byte-identical to an existing CSS variable's value. This is drift: the color is *already* on the design system, it's just hardcoded instead of referencing `var(--token)`. Auto-fixable.
   - **Off-palette** — the hex value has no corresponding token. Needs either a token added to `globals.css` (if it's a legitimate reused/semantic color) or a design decision (if it's a genuine one-off that should be replaced with a system color).
4. `--fix` mode replaces every quoted token-match literal with `var(--token)`, preferring the shortest `--color-*` alias over legacy duplicate variable names.
5. A literal on a line with an `audit-allow: <reason>` comment is a documented, deliberate exception (e.g. a color-picker default value or example placeholder text, not a styling decision) — excluded from the violation count but still listed below so it stays visible.

## Summary

| Metric | Count |
|---|---|
| Files scanned | 26 |
| Files with raw hex violations | 0 |
| Total hex literal violations | 0 |
| — matching an existing token (fixable) | 0 |
| — off-palette (needs a token or a decision) | 0 |
| Documented exceptions (`audit-allow`) | 8 |

## Per-file detail

No unresolved violations. Every page conforms to the design-system token set.

## Documented exceptions (`audit-allow`)

| File | Line | Hex | Reason |
|---|---|---|---|
| `src/app/admin/storefront-settings/page.tsx` | 239 | `#102542` | hex format example in a user-facing message, not a style token |
| `src/app/admin/storefront-settings/page.tsx` | 243 | `#8fa3b8` | hex format example in a user-facing message, not a style token |
| `src/app/admin/storefront-settings/page.tsx` | 452 | `#102542` | example placeholder text shown to the user, not a style token |
| `src/app/admin/storefront-settings/page.tsx` | 474 | `#102542` | hex format example in a user-facing message, not a style token |
| `src/app/admin/storefront-settings/page.tsx` | 488 | `#8fa3b8` | example placeholder text shown to the user, not a style token |
| `src/app/admin/storefront-settings/page.tsx` | 510 | `#8fa3b8` | hex format example in a user-facing message, not a style token |
| `src/app/design-engine/page.tsx` | 433 | `#000000` | literal default value for a <input type=color>, not a style token |
| `src/app/design-engine/page.tsx` | 482 | `#000000` | example placeholder text shown to the user, not a style token |

_Last run: 2026-07-03_
